#!/bin/bash
# AI Agent - LLM with command execution abilities
# Usage: ./agent "do something" or ./agent -i for interactive mode

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

python3 "$SCRIPT_DIR/mcp-server/agent.py" "$@"
